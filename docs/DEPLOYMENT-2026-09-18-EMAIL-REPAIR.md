# 2026-09-18 修复调价与邮件通知发布记录

用户明确要求部署后，已通过工程免密脚本发布当前工作区的源码。本次执行部署，未执行 Git 提交或推送。

## 发布及验证

- 入口：`bash deploy.sh --preserve-legacy-repair <已确认的旧修复任务ID>`。
- 源码包包含 297 个文件；服务器文件逐一匹配发布 SHA-256 清单。
- 服务端备份：`/home/ubuntu/epub-factory/deploy-backups/20260918T100750Z-3635341`。备份包含替换前代码、数据库、缓存、配置和依赖版本；凭据只留在服务器的私有备份中。
- Nginx、API、Celery worker、beat 均 active；公网 `/api/healthz` 返回 `status=ok`。
- 公网修复页返回 HTTP 200，显示新价 **0.99 元**，缓存要求重新验证。独立修复服务配置为 0.99，普通格式转换为 1.99；历史订单冻结金额未改写。

## 旧修复订单保护

部署前有一笔用户明确确认的真实用户修复任务，仅保存在旧 API 内存中。经过固定旧版源码哈希、单进程运行环境、5.99 元旧价和源文件核验后，在同一部署锁内暂停 Nginx、排空连接、读取最后状态并排他持久化元数据，再重启 API。

迁移仅延续本地 `pending_payment` 状态，没有把支付宝查单失败解释为未付款，也没有标记已付款、重试修复或调用模型。迁移证据单独保存在服务器私有备份中，不收入源码包。

重启后只读验证：原任务仍可读取，状态仍为 `pending_payment`、报价仍为 5.99，商户订单号相同，源文件 SHA-256 不变，元数据权限为 0600。数据库任务状态计数与发布前一致：待付款 80、成功 35、失败 8、取消 3。

旧版代码不读取持久化的修复元数据。因此回退时必须先确保使用可读取这些元数据的 API，不能仅恢复旧源码后就开放修复入口。专用发布脚本会在原任务不可读时保留维护状态。

## 邮件功能

- 顾客结果邮件与商户成交邮件的独立派发线程已在生产启动，持久化订阅表和成交待发队列存在。
- 公网邮箱能力接口返回 `available=true`；部署后没有补发历史成交，核验时商户待发队列为空。
- 此前 QQ SMTP TLS/认证通过，一封明确标为通道测试的邮件已被 QQ SMTP 接受。真实收件箱是否收到及首笔真实新成交的端到端通知，尚待分别确认；服务启动不等同于收件验收。

## 本次发布前检查

- D33 16 项：新价与旧订单报价、支付冻结、恢复显示、重启连续性。
- D22 19 项：备份、价格迁移、活动任务拦截。
- 旧任务迁移工具 19 项、Nginx 恢复保护 6 项，均使用离线替身。
- D35 付款邮件 23 项，以及此前完成的支付回调、顾客订阅、前端与配置脚本回归。
- Shell/JavaScript/Python 语法及 `git diff --check` 通过。没有伪造生产支付事件、发送真实顾客邮件或消费模型 Token。
