# 2026-09-17 生产发布验收

本文件为发布后的本地验收记录，不属于已上传发布包；记录生成时功能代码已部署，尚未 Git 提交或推送。后续 Git 交付结果以任务回报为准。

## 最终版本与模型

- 发布包含 224 个源码文件；服务器逐一校验，0 个不匹配。
- 生产 `OPENAI_MODEL`、`EPUB_DEFAULT_TRANSLATION_MODEL` 均为 `deepseek-flash`；主通道为 `api.deepseek.com`。通过现有授权只读查询模型列表，返回 200 且包含该模型名；没有发送书稿或调用推理接口。
- 官方 [更新日志](https://api-docs.deepseek.com/zh-cn/updates/)确认该名称对应 V4.1 Flash；两个旧 Flash 名称仅临时兼容。历史订单与模型相关缓存键未批量改写，已验证缓存仍按模型配置区分。
- 前端发布标记为 `20260917-flash-alias`，线上页面显示 DeepSeek V4.1 Flash；高质量说明已改为所选模型首译，不再声称自动使用 Pro。

## 交付连续性

- 126 个旧订单全部保留，支付金额、状态、访问令牌、会话及成品路径未改变。
- 译文缓存发布前后均为 48170 条；除已批准的四项模型/队列默认外，生产配置未变。
- 订单 `88c8dc5ba770` 列表可见、详情 `completed`，通过原授权获取新签名下载返回 200，4489964 字节，SHA-256 为 `eb9915f917ad97f69dabc305fd7c0592a0216e6599c47e67913a4e731d707fb0`，与发布前一致。未重译或改写 EPUB。
- 公网首页及版本化脚本均为 `no-cache, must-revalidate`；任务接口为 `no-store`。管理员匿名接口仍为 401、`no-store`。
- API、worker、beat、Nginx 均为 `active`，公网健康检查为 `ok`。

首次运行数据及入口配置备份：`/home/ubuntu/epub-factory/deploy-backups/20260917T142502Z-3345917`。最终源码/运行数据备份：`/home/ubuntu/epub-factory/deploy-backups/20260917T143624Z-3349393`。备份只在服务器保存，未复制生产凭据回本机。

## 两台 Mac

部署不依赖固定 Mac 用户名/工程目录，各用独立 SSH 密钥。新增服务器级 `.deploy.lock`，覆盖维护与发布全程。已在服务器持锁后使用不存在的发布包尝试第二次发布，确认在服务停止前明确拒绝；未触发维护操作。锁文件权限与源码发布权限隔离。

隔离 HOME、包含空格的路径及不同工作目录测试 2/2 通过；另一台 Mac 的真实免密连接未测试，也未替它生成或安装密钥。先提交并推送代码，另一台在干净工作区拉取同一提交后，按 [部署文档](DEPLOY.md)执行 `--check`。

## 验证与未完成事项

后端 31/31 套件通过（包含原书及历史章节样本）；前端 118 项通过、6 项失败，全部为既有 F9 预览/反馈功能；源码发布包测试 3/3 通过。验证未产生新的全书模型调用。

网址质检误报、246 条规则告警、4 个英文目录标签仍需后续复核；告警不等同于已证实的翻译错误。OCR 与图文重绘仍为 TODO，新模型支持多模态不代表工程已实现图片像素文字翻译。原用户浏览器刷新后的实际点击下载未验证；本次已验证会话/令牌保留测试、静态缓存配置和原授权公网下载，新会话不会自动获得历史订单权限。
