# 2026-09-18 剩余问题定位与无 Token 验证

> 下文保留历史诊断及第一阶段修复。后续用户指出两本原书均可在 Apple Books 阅读，进一步核对确认 Python 书正文齐全，原先“缺正文”的结论不准确；第一阶段对局部缺资源一律拒绝的规则已改为可恢复时告警继续。最新实现与两本实书验证见 [资源容错回归](EPUB-RESOURCE-TOLERANCE-2026-09-18.md)。

## 范围

按用户选择，先定位剩余问题并做不消耗 Token 的验证。本次不修业务代码、不部署、不重试订单、不发起真实支付或模型请求。

- 工作区：`main`，HEAD 与 `origin/main` 均为 `91f3008`。保留 pull 前恢复的本地改动及 `codex-before-pull-20260918` stash；本附录不表示这些改动已提交。
- 用户附件与仓库 `docs/BOOK-CORPUS-REGRESSION-2026-09-18.md` 内容一致。该报告的 15 本隔离转换数据作为既有证据，本机本轮没有重新转换全部 15 本。
- 本轮额外只读取得订单 `88c8dc5ba770` 的原文件与正式修正版，在本机私有临时目录验证，不修改服务器文件或订单。不输出书稿正文和凭据。
- 原书 SHA-256：`b5bcf437126c888f5f092a16590cdbfdf1d3ec2d787acf0ec7187536b6a709c4`。
- 正式修正版 SHA-256：`de02e6e4ab99dcac422857df4979d9f0cef8389b8ceab19ce7627b83159668b3`。远端读取前和本地副本均校验一致。

## 已复现的剩余问题

### 1. 优先修复：缺正文 EPUB 仍能进入付款流程

`backend/app/main.py:46` 的上传校验只有格式/扩展名与 PDF 文件头检查。缺正文的拒绝位于后续读取阶段的 `backend/app/engine/epub_compat.py:95`，尚未成为统一的付款前门禁。

使用 `test_d30_corpus_fixes.fixture(missing_body=True)`，临时 SQLite/上传目录、禁用 dotenv、禁止网络、模拟全部支付和入队方法，并启用正常付费分支（`SKIP_PAYMENT_CHECK=0`）：

| 本地接口场景 | HTTP 状态 | 支付替身调用 | 隔离库新订单 |
|---|---:|---:|---:|
| 单本普通转换 `/api/v2/jobs` | 200 | 1 | 1 |
| 单本翻译，`profile_confirmation=false` | 200 | 1 | 1 |
| 两本批量转换 `/api/v2/batches` | 200 | 1 | 2 |

执行入队调用均为 0。这里证明的是缺正文文件能创建订单并请求支付入口，不是声称本次发生了真实付款，也未据此推定某历史付款订单的原因。

后续修复应在所有建单/付款入口检查必需正文和被引用资源，返回明确的原文件问题。缺少可选 page-map、可回退字体等仍应使用既有兼容策略，不应一概拒绝所有源 EPUBCheck 报错文件。

临时复现脚本：`/tmp/epub_missing_resource_probe.py`；结果：`/tmp/epub-september-input-gate-results.json`。

### 2. 优先修复：校验工具不可用会被当作校验通过

动态调用正式转换器 `ExtremeCompiler._run_epubcheck()`：

| 故障注入 | 当前返回值 |
|---|---|
| EPUBCheck JAR 不存在 | `True` |
| Java 不存在（`FileNotFoundError`） | `True` |

对应 `backend/app/engine/compiler.py:438`、`:478`。`backend/app/domain/status_resolver.py:28` 依赖 `validation_passed`，因此普通转换存在未校验却成功的路径。

应区分“通过”“失败”“未能执行”，交付路径不能将第三种当作通过。本次部署预检成功，未证明现网缺少工具；这是已复现的代码边界问题。回归工具自身已将缺少 JAR 标为 `not_run`，但未覆盖正式转换器这条行为。

### 3. 大章回写仍存在反复扫描

`backend/app/domain/chapter_reduce_service.py:120` 每个块都调用 `get_node_by_locator`，其 `_get_direct_children` 每次扫描全部同名兄弟。提取器和回归审计已使用一次 DOM 索引，正式译文回写尚未同步优化。

合成同章段落，实际调用 `apply_chunk_results`，并断言输出段数、ID、译文全部一致：

| 同章段数 | 裸跑回写耗时 |
|---:|---:|
| 1000 | 0.0450 秒 |
| 3000 | 0.2403 秒 |
| 6000 | 0.8765 秒 |

6000 段另开 cProfile：分析器下总计 3.029 秒，定位占 2.452 秒（约 81%），同级节点扫描占 2.401 秒，`isinstance` 调用约 3660 万次。代码及计时支持该场景为二次复杂度。不能将分析器耗时与裸跑混用，也不能据此解释此前线上数小时耗时或外推整书提速倍数。

另有重复解析候选：`book_reduce_service.py:104` 加载书后，`:115` 又调用内部重新加载/分块的 `build_manifest`。尚未量化占比，不判为主要瓶颈。

### 4. 真实原书测试存在过期断言，报告统计需统一

补齐真实原书和正式译本后，D29 指纹测试在 `backend/test_d29_translation_performance.py:468` 失败：预期正文 2681 块，实际 2662 块。

已定位为 `cS.xhtml` 的 19 块（1 个标题、18 个含链接块）经首标题规则归为 `nav`。原书与修正版均如此；所有正文章节的块数一致；不存在 direct-text 新增块。仅在内存 mock 中退回旧的文件名分类，两份文件的正文统计均恢复 2681。

这是测试基线过期，不是发现少了 19 段正文。后续应同时断言 2662 正文块及 19 目录块，独立检查目录；不能只修改数字凑通过。

测试在旧计数断言处提前退出，因此另行直接验证其后两项指纹要求：跨 attempt 保持稳定、图片 HTML 改变后失效，均通过。本轮没有修改断言，原失败如实保留。

成品残留审计的 `text_blocks=2681` 与 Manifest 正文 `2662` 统计口径不同；报告应明确正文和书内目录范围，不能将不同口径直接比较为增删内容。

## 本轮无 Token 验证结果

| 专项 | 当前结果 |
|---|---|
| `backend/test_d30_corpus_fixes.py` | 32/32 通过 |
| `scripts/test_book_corpus.py` | 6/6 通过 |
| `scripts/test_deploy_portability.py` | 2/2 通过（模拟 Mac 路径/SSH/锁） |
| `backend/test_d29_translation_performance.py` | 28 项中 27 通过、1 项旧计数断言失败；最初跳过的 2 项已补跑 |

共 68 项不同用例，67 通过、1 失败；不将跳过或额外诊断计为通过。D29 覆盖持久化检查点、缓存复用、审校复用、跨中断请求预算、余额错误取消排队批次、超时等，模型均为替身。

真实书 20 段章节含 9 个图片混排段，回放既有已审阅译文：首次与恢复输出字节一致、标签/图片/链接/属性结构保持；恢复 20/20 段，新增模型请求 0。这是恢复逻辑验收，不是新的在线翻译验收。

正式修正版额外验证：

- 实际 Java + EPUBCheck 5.1.0：退出码 0，无 ERROR/FATAL，无警告消息。
- 原书/译本 2662 正文块对齐，定位失败 0；2637 规则通过，25 辅助告警（22 个数字提示、3 个关键标记提示），硬失败 0。
- v7 成品残留审计通过：检查 2681 个文本块，残留 0；52 个去重导航标签，导航残留 0，坏目标 0。使用此前用户确认的书级保留词 `KELVIN CHIU`。
- 上述结构与规则检查不能代替全书人工语言审校或阅读器显示验收。

临时证据：`/tmp/epub-september-offline-corpus.log`、`/tmp/epub-september-offline-audit.log`、`/tmp/epub-september-real-resume.log`、`/tmp/epub-september-reviewed-epubcheck.json`、`/tmp/epub-september-reviewed-audit.json`。临时证据不作为长期书稿登记册，不将书稿、数据库或审计原始数据提交 Git。

## 第二台 Mac 与文档

- 本机已安全拉取并核对远端提交，本地 Python 3.12.14 可运行上述回归。
- 实际执行 `bash deploy.sh --check` 成功：免密 SSH、服务器预检、Nginx 配置检查通过，检查时无 queued/running 订单。没有执行部署，不能称为一次发布验收。
- `docs/AI-TRANSLATION-DESIGN.md:86` 仍把真正断点续跑描述为后续扩展；`docs/QUALITY-FOLLOWUP-2026-09-17.md:85` 也保留旧的待办说法。当前已有检查点实现及上述离线证据，应补充更新并保留历史时间语义；不能把生产故障恢复在线验收同时标成完成。

## 仍需其他证据的事项

以下保留原报告的边界，不冒充本轮已验证或已修复：

1. `Python in a Nutshell 4` 原书缺正文；大书缺 3 张封面图片。程序不能补造客户原稿，需要完整文件。
2. 15 本书的真实 Flash 翻译质量、实际速度、费用及服务故障恢复尚无本轮在线证据。按本次选择不调用模型。
3. 洼地、regressdharem、丁庄梦、重返天安門的 4 类非致命警告仍需 Apple Books/Kindle 实际阅读检查。警告本身不等于排版错误，结构通过也不等于显示全部正确。
4. OCR/图片重绘仍为 TODO，PDF 保持关闭。

建议后续修复顺序：付款前完整性检查 → 校验不可用时禁止假成功 → 更新真实书回归口径和旧文档 → 回写索引优化。之后再单独进行有界的真实模型验收和阅读器人工验收。

## 后续修复与优化

用户随后授权“fix and 优化”。本阶段修改本地源码、测试、CI 和文档；不发起收费模型请求，不部署、不重提订单或替换客户成品。

### 付款前资源检查

新增 `app/domain/epub_input_integrity.py`。v1/v2 单本和批量入口在线程中检查上传流，恢复文件指针；全批次检查完成后才保存文件、创建订单或支付。画像待确认的旧订单在状态切换和创建支付前复检，拒绝时保留状态。

检查 ZIP/OPF、必需资源和本地引用；有解压大小、文档大小与文件数量上限，不抓取外部 URL，不传送正文。可选 page-map 与可安全回退的缺失阅读字体使用与实际解包器一致的规则，不能仅在入口假定后续可以修复。缺失 CSS、目录、正文、图片等必需资源提前明确拒绝。

独立审查另补齐三项实际读取边界：声明但缺失的 CSS/SMIL 等必需资源拒绝；门禁与实际解包器统一选择容器最后一个合法 OPF；检查 stylesheet、CSS `@import` 与图片，ZIP 中存在但未声明且最终会丢失的依赖同样拒绝。可选缺字体不是无条件豁免：只有无 OPF 依赖时才移除其缺失声明，保留已有字体；实际加载、完整转换与 EPUBCheck 通过后确认该兼容路径。

### 交付校验

新增共享 `app/engine/epub_validation.py`，普通转换、快速翻译和 SafeMode 使用同一验证器。初始不视为通过；只有实际 Java 进程成功、JSON 完整且计数一致、没有 ERROR/FATAL 才通过。缺 JAR/Java、超时、非零退出或报告损坏均不能交付，临时报告安全清理。

`EPUB_VALIDATION_UNAVAILABLE` 明确表示校验未能完成，QA 显示 blocked 且不可交付；真实格式错误为 `EPUB_VALIDATION_FAILED`。快译预处理未通过校验时，在画像、术语和翻译调用之前停止，避免继续产生模型费用。

### 回写性能

回写时先建立原始 DOM 索引并绑定节点，不再逐段重复扫描同级节点；保留省略 `[1]`、重复段落、单语/双语、链接和锚点规则，避免译文新增节点使后续 locator 偏移。

同机相同输入、三次计时中位数，输出字节完全一致：

| 合成同章段数 | 旧定位回写 | 索引回写 |
|---:|---:|---:|
| 1000 | 0.046843 秒 | 0.025315 秒 |
| 3000 | 0.242818 秒 | 0.078676 秒 |
| 6000 | 0.782473 秒 | 0.156468 秒 |

6000 段回写约快 5 倍，仅代表本机合成回写阶段，不能外推整书模型翻译。复测入口为 `backend/test_d31_reduce_index.py --benchmark`；没有将时间阈值作为跨机器 CI 断言。

### 回归与文档维护

- D21/D29 真实书测试同时记录 2662 正文块及 `cS.xhtml` 的 19 个目录块；额外验证原/译本正文逐章对应、目录链接、目录译文及指纹失效条件，没有只改数字。
- 真实书恢复测试继续回放已有译文，不请求模型。获准保留姓名的处理仅在这本书的测试中使用，不增加全局英文残留豁免。
- API 测试改用最小合法 EPUB。D3 用替身隔离后台执行，避免合成有效书进入真实模型请求；D3/D5 恢复纳入 CI。
- 更新旧的断点恢复文档，并修正与已有 Flash 不自动升级 Pro 约束冲突的测试，保留请求预算和失败处理断言。
- 新增付款前完整性、校验故障、回写索引三套专项测试，纳入 CI；原客户书籍、私钥、凭据、数据库和临时审计数据不进入 Git。

本阶段不宣称补齐原书缺失内容、完成全书人工审校或实际阅读器排版验收；PDF/OCR 边界不变。

### 修复后验证结果

新增三套专项全部通过，共 **53/53**：输入完整性 29、交付校验 18、回写索引 6。输入专项使用真实 Java/EPUBCheck，无跳过；覆盖“缺字体回退成品 0 错误”及“未声明 CSS/图片的旧输出确实产生 2 个错误，而新入口先拒绝”的对照。

- D29 **28/28**，包括原书与正式译本两项真实文件测试；20 段恢复全部复用、0 个新模型请求、输出与结构一致。
- D30 **32/32**；API D3 **37/37**、批次 **6/6**、画像确认 **8/8**；快译主链路 **8/8**（真实 Java，模型替身），翻译增强 **38/38**，Flash 别名/禁止自动升级 **6/6**。
- 其余订单、支付、QA、目录、媒体、恢复、缓存与回写相关套件通过；D21 有 1 项历史快照样本缺失、D27 有 1 项旧译本对照缺失，明确跳过，不计为通过。C1/C2 中依赖未提供 `test_en.epub` 的旧集成样例仍按原逻辑跳过，新的 D31 已独立覆盖真实校验及 SafeMode。
- 回归工具 **6/6**，源码打包 **3/3**，部署可移植性 **2/2**。本轮未执行部署。
- 修改的运行模块按 Python 3.10 语法解析通过，`git diff --check` 通过。本机测试运行时为 Python 3.12，未把这项语法检查当作生产 Python 3.10 完整运行验收。

北京时间约 09:47，用冻结后的入口检查模块在服务器独立 Python 进程内存中只读核对原报告的 24 个订单（同一取样截止时间），按 SHA 去重获得 **16 本 EPUB**：

| 结果 | 本数 | 证据 |
|---|---:|---|
| 入口接受 | 14 | 包括原报告可修复书及《市场奇才》原书，没有新增误拒 |
| 缺图片拒绝 | 1 | `6dbe0213a5b6`，原报告缺 3 张封面图的大书 |
| 缺正文/目录拒绝 | 1 | `3e78a0ad5605`，原报告缺正文的 Python 书 |

单书入口检查约 1.3–296 毫秒。这是 ZIP/资源检查耗时，不含转换或模型耗时；本轮没有重新转换整批书。16 份原书前后完整哈希一致；24 订单所核对的 status、updated_at、input_path、output_path 快照一致。代码仅存在独立进程内存，禁写 pyc，未上传部署代码、重启服务或修改生产数据。

本轮临时证据：`/tmp/epub-d31-production-source-gate.json`、`/tmp/epub-d31-input-final.log`、`/tmp/epub-d31-validation-final.log`、`/tmp/epub-d31-reduce-final.log`、`/tmp/epub-d31-d29-final.log`，以及 `/tmp/epub-d31-reduce-benchmark-20260918.jsonl`。新增代码与文档尚未 push 或部署。
