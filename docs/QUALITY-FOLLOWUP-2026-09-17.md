# 翻译质量逐项修复与回归记录

## 交付边界

先推送的基线为 `83cc5ed`；统一交付判定与网址误报修复为 `a17898f`。
2026-09-17 后续代码已完成本地验证并推送为 `3345db5`，当时尚未部署，也未替换生产订单的文件、访问令牌或翻译报告。
不能将程序验证通过等同于全书语义审稿通过。

## 已修复

- **交付判定冲突**：聚合 QA 决定是否交付；完整 URL/DOI 可保持原样，但夹带英文正文、危险协议及带凭据网址不能借此放行。仅需复核的规则警告不再显示为硬失败。
- **图片项目符号漏译**：原逻辑跳过任何含图片的整段，真实书稿因此漏掉 16 段（序言 9 段、交易建议 7 段）。现在提取图片外的文字，使用文本节点翻译，完整保留媒体子树；最终扫描覆盖同一范围。SVG 内部文字与图片像素不在此次翻译范围。
- **重复段落定位错误**：BeautifulSoup 的 Tag 相等比较按内容进行，旧 `siblings.index(tag)` 会把相同段落都指向首个相同节点。改用对象身份确定兄弟序号；嵌套的相同父容器也分别定位。真实译本另有 8 个短回答遗漏，已补齐。
- **短回答和姓名漏检**：明确的 Yes/Exactly 等短回答纳入共享残留检测；缩写、公式和用户明确确认的保留词不误拦。短英文姓名标题不能无确认放行，旧缓存读取和补译也保留原块级标题上下文。
- **目录未中文化**：复用确切的通用标签和书稿已有中文术语，不猜测姓名。更新标题时保留层级、目标链接和 ID；成品同时扫描 NCX、NAV 及目标锚点，对两份导航的相同问题去重。
- **规则误报**：数值按金额、百分比及数量级比较，保留负号、重复次数和币种约束；支持 `.5% = 0.5%`。脚注编号不再与前面的金额/小数粘连。中文短问句、姓名缩写、时间缩写、数字小数点及省略号不再制造截断或句数误报；区分时间性的 yet/thus far 和真实转折/因果。
- **真实反义误译**：`avoid using` 被译成“避免……不使用”，使含义反转。已定点修正；新增窄范围否定作用域复核信号，不自动改写未知段落。
- **预览及反馈缺失**：补齐受既有订单权限保护的本站章节预览、切章、沙箱和反馈。图片仅加载 EPUB 内的受限栅格资源，不执行书内脚本或远程资源；编码的 HTML 保持文本。反馈校验类型/长度/权限并限流，持久保存失败返回错误，关闭重开后旧响应不能清空新反馈。
- **文本节点策略一致性**：图片旁文字和结构化脚注继承锁定的章节策略、静态上下文。不引入请求完成后的串行滑动窗口更新。

## 真实书稿修正版

订单：`88c8dc5ba770`，*Market Wizards: The Next Generation*。
使用此前用户选定的原书及实际译本，未另选测试书、未调用模型或重新提交任务。
修订是逐段对照原文后的定点编辑，不是新一轮全书 DeepSeek 翻译。

原书 SHA-256：`b5bcf437126c888f5f092a16590cdbfdf1d3ec2d787acf0ec7187536b6a709c4`。
旧译本 SHA-256：`eb9915f917ad97f69dabc305fd7c0592a0216e6599c47e67913a4e731d707fb0`。
候选修正版 SHA-256：`ea5f11ae977bb0eb93346cada3ebf4f235b77aff81910cfe5c88cb16bee95c90`。

本地候选文件：`artifacts/translated-books/市场奇才：下一代_目录与语义修正版_待人名确认.epub`。
修订清单和确切目录映射保存在同一目录的 JSON 中；这些含书稿内容的本地工件不加入 Git。

- 正文提取从旧口径 2665 增至 2681 段；新增加的 16 段全部可独立定位。
- 2681 段逐一对照，定位失败为 0；当前 2655 段规则通过、25 段复核提示、1 个未确认的英文姓名标题。
- 57 个 ZIP 成员仅改变 7 个文字/导航文件，另外 50 个内容字节相同。所有标签结构、属性、图片、脚注链接、页码 ID、目录目的地均逐一核验不变。
- EPUBCheck：0 fatal / 0 error / 0 warning。
- 截至 2026-09-17，`KELVIN CHIU` 的正文标题及目录仍待用户确认：保留英文原名，或使用明确标为音译的“凯尔文·邱”。当时未擅自套用译名，也未关闭残留门禁，候选**不作为已通过最终 QA 的生产交付**。后续确认见 2026-09-18 记录。
- 原书题名页 George C. Coyle 与其他页面 George F. Coyle 不一致；原文写 `50–100k lots`。保留这些原书信息，不为降低警告而替作者纠错或擅改单位。

## 当前 25 条复核提示

逐条核对以下提示的原文/译文，未发现新的明确数字丢失或关系反转；保留提示，不制作未经批准的全局白名单。
这不是对历史 246 条警告的同口径重算：离线检查未复用当时自动术语表和所有上下文，不能直接覆盖生产统计。

| 类型 | 段落 ID | 复核依据 |
| --- | --- | --- |
| 年代及世纪写法（11 条） | c179_0270、c1GP_0010、c20E_0011、c20E_0087、c20E_0161、c2RX_0030、c38_0007、c4T_0017、c68_0201、cF2_0076、cF2_0293 | 英文完整年代与中文世纪/年代、世纪初写法不同，不能按字面数字认定漏译 |
| 疾病名中的编号（3 条） | c1GP_0147、c1RB_0214、c68_0064 | COVID-19 与“新冠”属于名称表达，不是交易金额；不普遍豁免带数字的专名 |
| 入门课程习语（2 条） | c1RB_0093、c68_0222 | trading 101-level 对应“交易入门级” |
| 比例/分数（3 条） | c179_0136、c26S_0239、cU0_0388 | 上半部分、万分之一、十分之一，数字表记不同 |
| 约数/年龄（2 条） | c1RB_0266、cF2_0266 | mid 80s 对应约 85、40s or 50s 对应四五十岁，需保留近似性 |
| 整点（1 条） | c20E_0065 | 9:00 对应 9 点，零分钟不是丢失数量 |
| 语义关系（3 条） | c1RB_0166、c2FB_0081、c2FB_0139 | “很难再低”“毫无进展”“事后才意识到”以中文句法保留限制、转折或之前未意识到的含义，不要求机械添加关键词 |

规则信号只辅助复核，反向翻译同样不能作为主标准。仍不能保证无信号段落都达到信达雅。

## 回归与浏览器核验

- 后端全套：36/36 套件通过，使用真实原书、11 份历史章节与候选译本。
- 前端全套：132/132 项通过；测试执行器现在等待异步测试，避免假通过。
- 发布包隔离：3/3；跨 Mac 路径及发布脚本：2/2。
- 新增重复节点、完整媒体保护、短回答、旧缓存重验、静态策略透传、定点修订版本保护及预览/反馈权限测试。
- 浏览器在 **本机隔离服务** 上使用真实候选书：刷新任务中心、打开 26 节预览、实际显示封面、序言 9 段补译、章节选择及交易建议已核验；西蒙章节中的 Yes/Exactly 残留均为 0。工具栏键盘切章与 Escape 关闭实测通过，不宣称沙箱内正文焦点下的箭头可跨 iframe 触发切章。测试服务使用临时订单数据、只接受 GET，不向生产提交反馈或任务。
- 本机 `.env` 默认模型改为 `deepseek-flash`，密钥未改、未进 Git。另一 Mac 的实际环境和原下单浏览器刷新下载尚未验证；不能将本地夹具验证算作生产用户验证。

复核命令（路径由各 Mac 自行提供）：

```sh
EPUB_REGRESSION_BOOK=/path/to/original.epub \
EPUB_REGRESSION_SNAPSHOTS=/path/to/historical-chapters \
EPUB_REGRESSION_PREVIOUS_TRANSLATED_BOOK=/path/to/previous.epub \
EPUB_REGRESSION_TRANSLATED_BOOK=/path/to/candidate.epub \
backend/.venv/bin/python -u backend/run_regression.py

backend/.venv/bin/python scripts/audit-translated-book.py \
  /path/to/original.epub /path/to/candidate.epub --report /path/to/review.json
```

## 下一步

1. 确认 KELVIN CHIU 的处理方式，重新做最终 QA，未通过前不替换生产文件。
2. 新代码部署前再检查生产任务空闲，保留现有订单/缓存/配置/成品备份；部署后验证服务和真实刷新下载。
3. 另一 Mac 拉取后验证 Python 环境、规范模型名及 SSH 发布权限。原书和本地译本不随 Git 同步。
4. 章节级真正断点续跑、生产多订单限流实测、更完整的全书语义/译名一致性审校仍待后续；不把换提示词或规则绿灯当作已完成这些工作。
5. OCR 与图文重绘仍是明确的后续 TODO，不在本轮实现。

## 2026-09-18 人名确认与发布准备

用户明确确认 **保留英文** `KELVIN CHIU`，并授权 push/deploy。
仅在该书的术语表中记录 `KELVIN CHIU → KELVIN CHIU`，不添加全局豁免、不放宽英文残留阈值。
书内印刷目录原有的“凯尔文·邱”同步改为 `Kelvin Chiu`；导航目录和正文英文标题保留。
保留姓名的术语检查允许正常大小写差异，仍要求完整名称、不能用姓氏子串替代。

- 正式修正版：`artifacts/translated-books/市场奇才：下一代_正式修正版.epub`。
- SHA-256：`de02e6e4ab99dcac422857df4979d9f0cef8389b8ceab19ce7627b83159668b3`。
- 2681/2681 段定位并复核：2656 段规则通过、25 段辅助复核、0 个硬失败；正文英文残留 0、目录残留 0、失效目录链接 0。
- 相对旧译本仅 8 个文本/导航成员改变、49 个成员内容字节不变；标签、图片、链接与 ID 按真实文件回归核验。
- 25 条提示仍按上文保留，不将交付规则通过等同于全书人工审稿完成。

`scripts/publish-reviewed-book.py` 默认只检查；加 `--apply` 才发布。
发布前核对原书/旧译本/候选三份校验值、任务空闲、完整成品与聚合 QA、EPUBCheck；与双 Mac 部署共用排他锁。
服务器本地备份数据库、旧文件与完整复核报告后，写入单独修正版并在数据库事务中切换该订单的文件指针。
再次检查订单/文件未变化、没有新运行任务；不改支付、访问权限、历史模型/API/Token/翻译段落计数，也不清空缓存。
旧失败明细按成品重新复核并保留历史审计，不让已消除的网址误报继续出现在刷新后的失败列表。
未经确认/版本不符/正在翻译/EPUBCheck 失败/订单并发变化均拒绝发布；已发布同版本可安全重试。

复核命令增加 `--preserve-term 'KELVIN CHIU'`；两个真实译本的回归命令增加 `EPUB_REGRESSION_EXPECTED_CHANGED_MEMBERS=8`。
原书、成品和含书稿内容的修订/复核 JSON 不加入 Git。
